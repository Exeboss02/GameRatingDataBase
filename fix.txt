DONE - Only 1 multirelation query - spec requires ≥2 with JOIN

DONE - main() always re-initializes the DB on every run - no setup/run separation, will duplicate or error on repeated runs

DONE - Genre1/Genre2 violates normalization - acknowledged but not justified

--------ONLY TEXT-------------------

No Discussion/Resources section, no references

Query explanations too thin (2–3 sentences, no motivation or call sites)

Missing function or a procedure

Queries shown as screenshots - should be text/code blocks (not copyable, not accessible)

No SQL schema / CREATE TABLE section (Task 3 missing)

Query 1 is a basic INSERT - spec explicitly says don't use these as one of your five queries

Query 2 is a trivial single-column PK lookup - weak choice for "interesting query"